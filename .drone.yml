kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64

steps:
  # - name: build and push
  #   image: plugins/docker
  #   settings:
  #     username:
  #       from_secret: DOCKER_USERNAME
  #     password:
  #       from_secret: DOCKER_PASSWORD
  #     repo: fullstacklab/ipstack
  #     tags: latest

  - name: deploy
    image: plugins/docker
    volumes:
      - name: docker_sock
        path: /var/run/docker.sock
    commands:
      - docker-compose up -d

volumes:
  - name: docker_sock
    host:
      path: /var/run/docker.sock
